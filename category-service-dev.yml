spring:
  #Database Configuration
  datasource:
    url: jdbc:mysql://localhost:3306/boaz-test?createDatabaseIfNotExist=true
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

product:
  service:
    url: http://localhost:8002
    
# Logging Configuration
logging:
  level:
    org.springframework.security: DEBUG
